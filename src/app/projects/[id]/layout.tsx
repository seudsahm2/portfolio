// Deprecated layout retained only to satisfy Next.js routing; dynamic project pages now redirect.
export default function LegacyProjectLayout({ children }: { children: React.ReactNode }) {
  return children;
}
